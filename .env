# Skopiuj do .env i uzupełnij
# cp .env.example .env

# Główna domena/hostname serwera poczty (np. mail.twojadomena.pl)
MAIL_HOSTNAME=mail

# Opcjonalnie: Let's Encrypt (wymaga montowania certyfikatów)
# SSL_TYPE=letsencrypt
# Volumes w compose: - /etc/letsencrypt:/etc/letsencrypt:ro

# Dla wielu domen: konta dodaj przez setup (patrz README)
# lub plik config/mailserver/postfix-accounts.cf
